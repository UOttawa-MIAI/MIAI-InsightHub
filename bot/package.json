{
  "name": "bot",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@google/genai": "^2.0.1",
    "discord-hono": "^0.21.2",
    "hono": "^4.12.18"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
